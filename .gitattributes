# Shell scripts must always check out with LF line endings — they run on
# the Linux VPS, and a CRLF shebang line (`#!/usr/bin/env bash\r`) fails
# with "bad interpreter: No such file or directory". This is enforced
# regardless of any contributor's local core.autocrlf setting.
*.sh text eol=lf
